/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./js/main.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./js/main.js":
/*!********************!*\
  !*** ./js/main.js ***!
  \********************/
/*! no static exports found */
/***/ (function(module, exports) {

var video = document.querySelector('.gym__video-wrapper');
var tabTitle = document.querySelectorAll('.season-tickets__nav-item');
var tabContent = document.querySelectorAll('.season-tickets__wrap');
var myName = document.getElementById('name');
var myPhone = document.getElementById('telephone');
var contactsForm = document.querySelector('.contacts__form');
var rephone = /^\d+$/;
var rename = /^[\t-\r 'A-Za-z\xA0\u0410-\u044F\u1680\u2000-\u200A\u2028\u2029\u202F\u205F\u3000\uFEFF]+[\t-\r 'A-Za-z\xA0\u0410-\u044F\u1680\u2000-\u200A\u2028\u2029\u202F\u205F\u3000\uFEFF]*$/; // Удаляю заставку и кнопку и меняю на айфрейм

if (typeof video !== 'undefined' && video !== null) {
  var media = video.querySelector('.gym__media'); // Картинка

  var button = video.querySelector('.gym__button'); // Кнопка

  if (typeof (media && button) !== 'undefined' && media && button !== null) {
    var setupVideo = function setupVideo() {
      button.addEventListener('click', function () {
        var iframe = createIframe();
        media.remove();
        button.remove();
        video.appendChild(iframe);
      });
    }; // Создаю айфрейм


    var createIframe = function createIframe() {
      var iframe = document.createElement('iframe');
      iframe.setAttribute('allowfullscreen', '');
      iframe.setAttribute('allow', 'autoplay');
      iframe.setAttribute('src', generateURL());
      iframe.classList.add('gym__ambed');
      return iframe;
    };

    var generateURL = function generateURL() {
      var query = '?rel=0&showinfo=0&autoplay=1';
      return 'https://www.youtube.com/embed/9TZXsZItgdw' + query;
    };

    setupVideo();
  }
}

if (typeof (tabTitle && tabContent) !== 'undefined' && tabTitle && tabContent !== null) {
  // Удаляю класс с табов
  var getDeliteTabTitleCurrentStyle = function getDeliteTabTitleCurrentStyle() {
    tabTitle.forEach(function (item) {
      item.classList.remove('season-tickets__nav-item--current');
    });
  }; // Удаляю класс с контента


  var getDeliteTabContentCurrentStyle = function getDeliteTabContentCurrentStyle() {
    tabContent.forEach(function (item) {
      item.classList.remove('season-tickets__wrap--current');
    });
  }; // Добавляю класс табу


  var getAddTabTitleCurrentStyle = function getAddTabTitleCurrentStyle(item) {
    var numb = tabTitle.length;

    for (var i = 0; i < numb; i++) {
      if (i === item) {
        tabTitle[i].classList.add('season-tickets__nav-item--current');
      }
    }
  }; // Добавляю класс контенту


  var getAddTabContentCurrentStyle = function getAddTabContentCurrentStyle(item) {
    var numb = tabContent.length;

    for (var i = 0; i < numb; i++) {
      if (i === item) {
        tabContent[i].classList.add('season-tickets__wrap--current');
      }
    }
  }; // Навешиваю клик на табы для открытия нужного контента


  tabTitle.forEach(function (item) {
    item.addEventListener('click', function (evt) {
      var numb = tabTitle.length;
      evt.preventDefault();

      for (var i = 0; i < numb; i++) {
        if (evt.currentTarget === tabTitle[i]) {
          getDeliteTabTitleCurrentStyle();
          getDeliteTabContentCurrentStyle();
          getAddTabTitleCurrentStyle(i);
          getAddTabContentCurrentStyle(i);
          break;
        }
      }
    });
  });
} // Сохранения данных в localStorage


function persist(event) {
  var thisArg = event.path[0];
  localStorage.setItem(thisArg.id, thisArg.value);
}

document.querySelectorAll('input').forEach(function (inputEl) {
  inputEl.value = localStorage.getItem(inputEl.id);
  inputEl.addEventListener('change', persist);
}); // Валидация Имени

if (typeof myName !== 'undefined' && myName !== null) {
  var checkNameValidity = function checkNameValidity() {
    var textName = myName.value;

    if (textName === '') {
      myName.setCustomValidity('поле обязательное');
    } else if (!rename.test(textName)) {
      myName.setCustomValidity('имя может содержать только буквы');
    } else {
      myName.setCustomValidity('');
    }

    myName.reportValidity();
  };

  myName.addEventListener('input', checkNameValidity);
} // Валидация телефона


if (typeof myPhone !== 'undefined' && myPhone !== null) {
  var checkPhoneValidity = function checkPhoneValidity() {
    var textPhone = myPhone.value;

    if (textPhone === '') {
      myPhone.setCustomValidity('поле обязательное');
    } else if (!rephone.test(textPhone)) {
      myPhone.setCustomValidity('номер телефона должен содержать только цифры');
    } else {
      myPhone.setCustomValidity('');
    }

    myPhone.reportValidity();
  };

  myPhone.addEventListener('input', checkPhoneValidity);
} //  Отправка формы


if (typeof contactsForm !== 'undefined' && contactsForm !== null) {
  var handleFormSubmit = function handleFormSubmit(evt) {
    evt.preventDefault();
    var formData = new FormData(evt.target);
    fetch('https://echo.htmlacademy.ru/', {
      method: 'POST',
      body: formData
    });
    evt.target.reset();
  };

  contactsForm.addEventListener('submit', handleFormSubmit);
} // Слайдер


var promoButton = document.querySelector('.promo__button');
var coachesButtonPrev = document.querySelector('.coaches__button-prev');
var coachesButtonNext = document.querySelector('.coaches__button-next'); // const imageSlidesFirst = document.querySelector('.image-slider__slide-1');

var imageSlidesTwo = document.querySelectorAll('.image-slider__slide-2');
var imageSlideThree = document.querySelector('.image-slider__slide-3'); // const imageSlideLast = document.querySelector('.image-slider__slide-8');

var promotionsMainButton = document.querySelector('.promotions__main-button');

if (typeof (promoButton && coachesButtonPrev) !== 'undefined' && promoButton && coachesButtonPrev !== null) {
  promoButton.addEventListener('keydown', function (evt) {
    if (!evt.shiftKey && evt.key === 'Tab') {
      evt.preventDefault();
      coachesButtonPrev.focus();
    }
  });
  coachesButtonPrev.addEventListener('keydown', function (evt) {
    if (evt.shiftKey && evt.key === 'Tab') {
      evt.preventDefault();
      promoButton.focus();
    }
  });
}

if (typeof (coachesButtonNext && imageSlideThree) !== 'undefined' && coachesButtonNext && imageSlideThree !== null) {
  coachesButtonNext.addEventListener('keydown', function (evt) {
    if (!evt.shiftKey && evt.key === 'Tab') {
      evt.preventDefault();
      imageSlideThree.focus();
    }
  });
  imageSlideThree.addEventListener('keydown', function (evt) {
    if (evt.shiftKey && evt.key === 'Tab') {
      evt.preventDefault();
      coachesButtonNext.focus();
    }
  });
}

if (typeof (imageSlidesTwo && promotionsMainButton) !== 'undefined' && imageSlidesTwo && promotionsMainButton !== null) {
  imageSlidesTwo.forEach(function (imageSlideTwo) {
    imageSlideTwo.addEventListener('keydown', function (evt) {
      if (!evt.shiftKey && evt.key === 'Tab') {
        evt.preventDefault();
        promotionsMainButton.focus();
      }

      promotionsMainButton.addEventListener('keydown', function (event) {
        if (event.shiftKey && event.key === 'Tab') {
          event.preventDefault();
          imageSlideTwo.focus();
        }
      });
    });
  });
}

/***/ })

/******/ });
//# sourceMappingURL=main.min.js.map